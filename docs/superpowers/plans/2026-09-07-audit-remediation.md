# 审查缺陷修复计划

依据：docs/audits/2026-09-07-project-review.md；用户已批准按审查建议修复。

目标：修复R1–R19可由代码解决的缺陷，为缺失的历史时点数据和HTTPS服务地址建立明确输入要求。保留纯研究工具定位，不新增交易执行，不伪造历史数据，不自动部署。

## 工作边界

- 回测：backtest_v2.py及成交因果回归测试。先复现同日移动止损、跳空、跌停与开盘准入，再统一按开盘、盘中、收盘顺序处理。
- 数据：main.py、local_data_proxy.py、data_downloader.py；验证缓存隔离、财务历史合并和股票状态有效期；缺历史属性时明确阻断可信回测。
- Web：app.py、模板、网关与缓存服务；修复统计口径、路径编码、轮询和并发；远端写入要求认证，GET解释不再发起AI。
- 编排：update_service.py/worker.py与deploy脚本；任务记录按模式保留、执行跨进程互斥、传递退出码；依赖与测试时钟独立修复。

## 验收步骤

- [x] 为回测和数据缺陷增加失败用例并验证修复。
- [x] 以临时目录和真实子进程验证失败码、并发互斥、不同模式成功记录。
- [x] 用TestClient验证网关启动、鉴权、统计样本、只读解释和缓存竞态。
- [x] 修复旧测试契约与未冻结时钟，隔离普通测试网络及新增回归的数据目录；既有页面冒烟测试的本地数据依赖在修复说明中保留边界。
- [x] 运行涉及模块的测试组合；针对失败追踪根因，不以弱化断言通过。全量907通过、22原有跳过，前端脚本3通过。
- [x] 更新配置/数据准备说明与审查关闭记录，标明需要外部数据或部署配置的事项。

所有实现组独立维护文件，整合时检查跨模块接口。当前工作区已有bf8bac1快照，修复保留为可审查差异，完成后由用户决定发布。
