# 浏览器流程修复记录（2026-09-07）

对应 [浏览器操作审查](2026-09-07-browser-flow-review.md) 的 B-01 至 B-08。实现以挂载在 `/stock` 的 `deploy.gateway:app` 为目标；自动化验证使用临时数据库和固定页面快照，不读取开发机真实业务库，也不调用行情或 AI 接口。

| 编号 | 根因与处理 | 回归覆盖 |
|---|---|---|
| B-01 | 单品种详情按 `stock/index/fund` 分支渲染，空基本面、行情、资金和财务字典先规范为空对象；指数缺少 `index_basic` 时用规范别名或查询词补名称，并允许显式代码从已有 `index_daily` 判定资产类型；全新空库缺少日线、资金、财务或信号表时返回空态；指数价格单位改为“点”；指数和 ETF 隐藏仅适用于股票的估值、策略与历史信号区。 | 临时 SQLite 分别构造指数、ETF 和完全空库，验证中文别名、显式 `000300.SH`、普通股票代码、无效输入及挂载地址 `/stock/stock/000300.SH` 均返回可读页面，指数名称/单位正确且未找到的股票不继续查询信号表。 |
| B-02 | `.chart-close` 与图例改用已定义的 `--blue`；静态 CSS URL 改用文件内容 SHA-256 短哈希，发布后旧浏览器会请求新资源。 | 验证 CSS 收盘线不是 `stroke:none`，并验证页面引用的版本等于当前 CSS 内容哈希。 |
| B-03 | 首页服务返回的动态链接统一经过 `app_path`，保留 ASGI `root_path`。 | 在 `/stock` 挂载子应用，验证中央体检入口为 `/stock/stock/000001`。 |
| B-04 | 数据页接入统一状态面板和装饰后的数据对齐状态；更新按钮通过单一轮询器更新运行、完成和错误状态。POST 失败会清除待刷新标记并恢复按钮；提交错误激活后，迟到或周期性旧 GET 不再覆盖错误，直到用户重试。 | Node 运行验证覆盖断网退避、单例轮询、403、迟到 GET、成功完成只刷新一次；页面测试覆盖状态面板属性和装饰文案。 |
| B-05 | 将深色侧栏链接规则限定为 `.sidebar nav`，正文 `.page-section-nav` 单独使用深色文字。 | CSS 回归检查选择器作用域及正文链接颜色。 |
| B-06 | 按页面数据日生成历史截面上下文；过期首页、短线、龙头、长线和板块文案改为“截至日期的历史判断”“该历史日”“下一交易日（历史）”。旧 AI 摘要正文保持原样，在外层显示来源日期和“不能作为当前建议”的警告。短线、龙头和长线历史统计均注明尚未按本次修复重算。 | 固定旧日期上下文验证页面不再把旧结果包装为当天扫描结论，覆盖长线历史标题、旧 AI 缓存警告及三个历史统计区的重算状态提示。 |
| B-07 | 401、只读写入和跨站写入统一返回中文说明；解释刷新改为页面内异步提交，失败时保留旧解释、恢复按钮并展示服务端中文错误。 | FastAPI 权限响应断言；Node 模拟解释 POST 403，验证不离开页面且显示错误。 |
| B-08 | 新增统一日期解析与区间验证，短线、长线和日报归档在服务端拒绝倒置区间并跳过数据查询；三个表单同时做浏览器端即时校验。 | 逐页验证中文错误、空查询调用，以及模板端日期检查脚本。 |

浏览器复核追加发现也已纳入：旧 AI 摘要历史警告、CSS 缓存失效、指数名称和“点”单位、非股票信号区隐藏，以及更新 403 与迟到状态请求的竞争条件。

## 验证结果

- Python 浏览器专项：`12 passed`。
- Web、雷达、龙头、历史查询与解释服务相关回归：`145 passed`。
- Node 页面脚本运行测试：`5 passed`。
- Python 语法编译与 `git diff --check`：通过（仅有仓库既有的 LF/CRLF 转换提示）。

## 未完成边界

代码项没有遗留。真实网关进程需要在文件稳定后重启，才能加载新模板全局函数并让浏览器取得新 CSS 哈希；真实浏览器复核、提交、推送和部署由主任务统一完成。真实外部行情更新和付费 AI 生成未在本次修复验证中执行。
