# 按用户确认恢复原行情中转

用户明确表示服务商没有 HTTPS 地址，要求按照原配置继续使用。因此恢复默认行情地址 `http://111.170.34.57:8010`，取消对该原地址的 HTTPS 强制要求；环境变量仍可覆盖为服务方 HTTPS 地址。

校验仍拒绝不相关的 HTTP 地址、嵌入用户名/密码、查询参数和片段。Token 仍只从环境变量读取，不写入仓库。启动日志按实际协议显示，不再把 HTTP 连接描述为 HTTPS。

本次仅调整行情地址配置，不修改算法和网页权限。此前发布记录中“等待 HTTPS 地址”的阻塞已由本次用户确认替代。

验证：行情配置、中转错误处理、数据完整性与日常更新相关测试 40 passed，5 subtests passed。生产连接验证结果将在完成后记录。
