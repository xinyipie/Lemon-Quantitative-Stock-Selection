# Environment variable example
#
# Do not commit real API keys. Keep real values in your shell, user environment,
# or a local .env file that is ignored by Git.

# Tushare API token
TUSHARE_TOKEN=your_tushare_token_here
# 用户确认沿用原行情中转；留空也使用此默认值，可覆盖为服务方HTTPS地址。
TUSHARE_HTTP_URL=http://111.170.34.57:8010

# 对外部署时配置长随机令牌；不要将真实值提交到Git。
STOCK_WEB_TOKEN=
# 仅开发机显式开启；公网部署应设置令牌并保持为0。
STOCK_WEB_ALLOW_LOCAL_WRITE=0
# 可选：产品原型静态目录，未配置时网关仍可展示选股应用。
STOCK_PRODUCT_WORK_DIR=

# DeepSeek API key
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Optional: DashScope API key if you switch AI_CONFIG back to Qwen
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# Windows PowerShell:
#   $env:TUSHARE_TOKEN="your_token"
#   $env:DEEPSEEK_API_KEY="your_key"
#
# Windows CMD:
#   set TUSHARE_TOKEN=your_token
#   set DEEPSEEK_API_KEY=your_key
#
# Linux/Mac:
#   export TUSHARE_TOKEN="your_token"
#   export DEEPSEEK_API_KEY="your_key"
